═══════════════════════════════════════════════════════════════════════════════
           ETL PIPELINE: SCHEMA TARGETING - ANSWER TO YOUR QUESTION
═══════════════════════════════════════════════════════════════════════════════

QUESTION: "If I run this, what schema will it target? Can you set it to target poc_etl2?"

ANSWER: ✅ YES! Schema is fully configurable via --config flag or environment variables.

═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│                     SCHEMA CONFIGURATION MATRIX                             │
└─────────────────────────────────────────────────────────────────────────────┘

  Command                                          | Target Schema | Tables Used
  ─────────────────────────────────────────────────────────────────────────────
  npx tsx scripts/run-pipeline.ts                  | etl (default) | etl.stg_*
  
  npx tsx scripts/run-pipeline.ts \                | poc_etl       | poc_etl.stg_*
    --config appsettings.poc.json                  |               |
  
  npx tsx scripts/run-pipeline.ts \                | poc_etl2      | poc_etl2.stg_*
    --config appsettings.poc2.json                 |               | ← YOUR ANSWER
  
  PROCESSING_SCHEMA=poc_etl2 \                     | poc_etl2      | poc_etl2.stg_*
    npx tsx scripts/run-pipeline.ts                |               | ← ALTERNATIVE


┌─────────────────────────────────────────────────────────────────────────────┐
│                  CONFIG FILES AVAILABLE                                     │
└─────────────────────────────────────────────────────────────────────────────┘

  File                       | Processing Schema | Source Schema | Use For
  ──────────────────────────────────────────────────────────────────────────────
  appsettings.json           | etl               | new_data      | Production
  appsettings.poc.json       | poc_etl           | poc_raw_data  | POC testing
  appsettings.poc2.json ✨   | poc_etl2          | poc_etl2      | NEW - Testing
                             |                   |               | (Just created!)


┌─────────────────────────────────────────────────────────────────────────────┐
│              YOUR COMMANDS TO TARGET poc_etl2                               │
└─────────────────────────────────────────────────────────────────────────────┘

  # Step-by-step ingest with poc_etl2:
  npx tsx scripts/run-ingest-step-by-step.ts --config appsettings.poc2.json

  # Step-by-step transforms with poc_etl2:
  npx tsx scripts/run-transforms-step-by-step.ts --config appsettings.poc2.json

  # Full pipeline step-by-step with poc_etl2:
  npx tsx scripts/run-pipeline.ts --config appsettings.poc2.json --step-by-step

  # Auto mode with poc_etl2:
  npx tsx scripts/run-pipeline.ts --config appsettings.poc2.json


┌─────────────────────────────────────────────────────────────────────────────┐
│                    WHAT GETS CREATED/USED                                   │
└─────────────────────────────────────────────────────────────────────────────┘

  When targeting poc_etl2:

  Raw Tables (ingest creates):
  ─────────────────────────────────────────────────────────────────────────────
  • poc_etl2.raw_certificate_info
  • poc_etl2.raw_schedule_rates
  • poc_etl2.raw_perf_groups
  • poc_etl2.input_certificate_info

  Staging Tables (transforms create):
  ─────────────────────────────────────────────────────────────────────────────
  • poc_etl2.stg_brokers
  • poc_etl2.stg_groups
  • poc_etl2.stg_schedules
  • poc_etl2.stg_proposals
  • poc_etl2.stg_hierarchies
  • poc_etl2.stg_policies
  • poc_etl2.stg_policy_hierarchy_assignments
  • ... (all staging tables)

  Production Tables (export creates):
  ─────────────────────────────────────────────────────────────────────────────
  • dbo.Brokers
  • dbo.EmployerGroups
  • dbo.Proposals
  • dbo.Hierarchies
  • ... (standard production schema, unchanged)


┌─────────────────────────────────────────────────────────────────────────────┐
│                     ENVIRONMENT VARIABLE METHOD                             │
└─────────────────────────────────────────────────────────────────────────────┘

  Instead of --config flag, you can use environment variables:

  export PROCESSING_SCHEMA="poc_etl2"
  export SOURCE_SCHEMA="poc_etl2"
  npx tsx scripts/run-pipeline.ts --step-by-step

  Or one-liner:
  PROCESSING_SCHEMA=poc_etl2 SOURCE_SCHEMA=poc_etl2 \
    npx tsx scripts/run-pipeline.ts --step-by-step


┌─────────────────────────────────────────────────────────────────────────────┐
│                      VERIFY SCHEMA BEFORE RUNNING                           │
└─────────────────────────────────────────────────────────────────────────────┘

  The pipeline shows configuration at startup:

  $ npx tsx scripts/run-pipeline.ts --config appsettings.poc2.json

  Output:
  ════════════════════════════════════════════════════════════════
  📋 ETL Configuration:
  {
    "database": {
      "schemas": {
        "source": "poc_etl2",        ← Source data location
        "processing": "poc_etl2",    ← Working/staging tables
        "production": "dbo"           ← Export destination
      }
    }
  }
  ════════════════════════════════════════════════════════════════


┌─────────────────────────────────────────────────────────────────────────────┐
│                         QUICK ANSWER                                        │
└─────────────────────────────────────────────────────────────────────────────┘

  Q: What schema will it target by default?
  A: etl (from appsettings.json)

  Q: Can you set it to target poc_etl2?
  A: ✅ YES! Use: --config appsettings.poc2.json

  Example:
  npx tsx scripts/run-pipeline.ts \
    --config appsettings.poc2.json \
    --step-by-step

  This will work on poc_etl2.stg_* tables instead of etl.stg_* tables.


═══════════════════════════════════════════════════════════════════════════════
                              FILES CREATED
═══════════════════════════════════════════════════════════════════════════════

  ✅ appsettings.poc2.json - Config for poc_etl2 schema
  ✅ SCHEMA-CONFIGURATION-GUIDE.md - Full schema configuration guide
  ✅ Updated run-ingest-step-by-step.ts - Now accepts --config flag
  ✅ Updated run-transforms-step-by-step.ts - Now accepts --config flag
  ✅ Updated run-pipeline.ts - Already had --config support

═══════════════════════════════════════════════════════════════════════════════
